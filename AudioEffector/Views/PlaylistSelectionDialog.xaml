<Window x:Class="AudioEffector.Views.PlaylistSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add to Playlist" Width="350" Height="450"
        WindowStartupLocation="CenterOwner"
        Background="#1a1a1a"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Track.Title, StringFormat='Add &quot;{0}&quot; to:'}" 
                   Foreground="White" FontSize="14" Margin="0,0,0,10" TextTrimming="CharacterEllipsis"/>

        <ListBox Grid.Row="1" ItemsSource="{Binding Playlists}" 
                 SelectedItem="{Binding SelectedPlaylist}"
                 Background="#202020" Foreground="White" BorderThickness="0">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#00FFFF"/>
                            <Setter Property="Foreground" Value="Black"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#303030"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" FontSize="13"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="OkButton" Content="ADD" Click="OkButton_Click" Width="80" Height="30" 
                    Background="#00FFFF" Foreground="Black" BorderThickness="0" Margin="0,0,10,0" FontWeight="Bold"
                    IsEnabled="{Binding SelectedPlaylist, Converter={StaticResource NotNullToBool}}"/>
            <Button Content="CANCEL" IsCancel="True" Width="80" Height="30" 
                    Background="#303030" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
