<UserControl x:Class="AudioEffector.Views.ShortcutInputBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="300"
             x:Name="root">
    <StackPanel Margin="0,0,0,15">
        <TextBlock Text="{Binding Label, ElementName=root}" Margin="0,0,0,5" Foreground="{DynamicResource SecondaryTextForegroundBrush}"/>
        <TextBox x:Name="InputTextBox" 
                 Width="250" HorizontalAlignment="Left" Padding="5" 
                 IsReadOnly="True" 
                 Focusable="True"
                 Cursor="Hand"
                 PreviewKeyDown="InputTextBox_PreviewKeyDown"
                 PreviewMouseDown="InputTextBox_PreviewMouseDown"
                 GotKeyboardFocus="InputTextBox_GotKeyboardFocus"
                 LostKeyboardFocus="InputTextBox_LostKeyboardFocus"
                 ToolTip="ここをクリックしてキーボードを入力するとショートカットを割り当てます。Delete/BackSpaceでクリア。"/>
        <TextBlock x:Name="WarningTextBlock" 
                   Foreground="#FFAA00" 
                   FontSize="12" 
                   Margin="0,5,0,0" 
                   Visibility="Collapsed" 
                   TextWrapping="Wrap"/>
    </StackPanel>
</UserControl>
