# インフラストラクチャ層 詳細設計書

## 1. 概要

### 1.1 目的
本ドキュメントは、AudioEffector における **インフラストラクチャ層（Infrastructure Layer）** の設計仕様書です。
インフラストラクチャ層は、外部ライブラリ（NAudio, TagLibSharp, MediaDevices）、ファイルシステム（JSONファイル）、およびNLogロギングとの具体的な連携（アダプター実装およびリポジトリ具象クラス）を担当します。

### 1.2 設計原則
1. **インターフェースへの適合**: Domain層またはApplication層で定義されたインターフェース（`ITrackRepository`, `IAudioEngine`, `IDataTransferAdapter` 等）を忠実に実装する。
2. **外部仕様のカプセル化**: サードパーティ製ライブラリの型や例外を上位レイヤーに漏洩させず、インフラ層内でドメインモデルや結果型（Result）へ変換する。
3. **プレゼンテーション層非依存**: WPF特有のコントロールやUIバインディングロジックを含めない。
4. **非同期メソッドの命名規則**: インフラストラクチャ層で提供する非同期メソッドは、**必ず末尾に `Async` を付与する**（例: `GetByIdAsync`, `SaveAsync`, `TransferTrackAsync`, `PlayAsync` 等）。

---

## 2. 名前空間とディレクトリ構造

インフラストラクチャ層は `AudioEffector.Infrastructure` をルート名前空間とし、連携対象ごとにサブ名前空間を構成します。

```
AudioEffector/
└── Infrastructure/                              # [名前空間: AudioEffector.Infrastructure]
    ├── Audio/                                   # NAudioを利用した音声再生エンジン・DSP処理
    │   ├── AudioService.cs                      # IAudioService の具象実装
    │   ├── NAudioPlaybackEngine.cs              # IAudioEngine の具象実装
    │   ├── SampleAggregator.cs                  # FFT解析用サンプルバッファ
    │   └── EqualizerDsp.cs                      # イコライザーDSPチェーン
    ├── Library/                                 # ファイルメタデータ解析・画像読み込み
    │   ├── TagLibMetadataExtractor.cs           # TagLibSharpによるID3/FLAC解析
    │   └── AlbumArtLoader.cs                    # 画像バイトデータ・Streamキャッシュ
    ├── DataTransfer/                            # MediaDevicesを利用したMTPポータブル機器データ転送
    │   └── MtpDataTransferAdapter.cs            # IDataTransferAdapter の具象実装
    ├── Repository/                              # JSON永続化リポジトリ具象実装
    │   ├── JsonPlaylistRepository.cs            # IPlaylistRepository の具象実装
    │   ├── JsonFavoriteRepository.cs            # IFavoriteRepository の具象実装
    │   ├── JsonPresetRepository.cs              # IPresetRepository の具象実装
    │   ├── JsonSettingsRepository.cs            # ISettingsRepository の具象実装
    │   └── JsonTrackRepository.cs               # ITrackRepository の具象実装
    ├── Windows/                                 # Windows OS連携
    │   └── StartupManager.cs                    # OS自動起動レジストリ制御
    └── Logging/                                 # ロギング基盤
        ├── NLogService.cs                       # NLogロギングサービス
        ├── LoggingProxy.cs                      # メソッド呼び出し透過的ロギングプロキシ
        └── LogDescriptionAttribute.cs           # ログ出力用説明属性
```

---

## 3. 各コンポーネントの配置基準と実装ルール

### 3.1 Audio (`AudioEffector.Infrastructure.Audio`)
- **配置基準**: NAudioに直接依存するオーディオデバイス制御、PCMバッファ管理、FFT解析、イコライザーDSP。
- **実装ルール**:
  - `Application.Common.IAudioEngine` を実装する。
  - 音声ストリームの初期化、再生（`PlayAsync`）、一時停止（`PauseAsync`）、停止（`StopAsync`）、シーク（`SeekAsync`）、音量制御（`SetVolumeAsync`）をカプセル化する。

### 3.2 Library (`AudioEffector.Infrastructure.Library`)
- **配置基準**: 音声ファイルからID3タグやFLACヘッダーを読み出すメタデータ抽出処理、およびジャケット画像ロード。
- **実装ルール**:
  - TagLibSharpを用いて楽曲情報を抽出し、Domainの `Track` / `Album` オブジェクトを構築して返却する（`ExtractMetadataAsync`）。
  - 画像データはWPFの `BitmapImage` に直接変換せず、`byte[]` または `Stream` として返す（WPF用変換はPresentation層で実施）。

### 3.3 Repository (`AudioEffector.Infrastructure.Repository`)
- **配置基準**: JSONファイルやローカルディスクに対するデータの保存・読み込みリポジトリ具象クラス。
- **実装ルール**:
  - `Domain.Repositories.IxxxRepository` を実装する。
  - `System.Text.Json` を用いて非同期メソッド（`GetByIdAsync`, `SaveAsync`, `GetAllAsync`, `DeleteAsync` 等）として実装し、ファイル破損対策（書き込み時の一時ファイル使用）を講じる。

### 3.4 DataTransfer (`AudioEffector.Infrastructure.DataTransfer`)
- **配置基準**: MediaDevicesライブラリを用いたMTPポータブルオーディオ機器へのファイル転送・ディレクトリ探索アダプター。
- **実装ルール**:
  - 端末の接続・切断検知、ファイル一覧取得（`GetDeviceFilesAsync`）、ファイルコピー転送（`TransferFileAsync`）を非同期メソッドとして提供する。

### 3.5 Windows (`AudioEffector.Infrastructure.Windows`)
- **配置基準**: Windows OS固有の機能（レジストリ操作等）との連携クラス。
- **実装ルール**:
  - `StartupManager.cs`: レジストリ（`Run`キー）への実行可能ファイルパス登録・削除により、OS起動時の自動起動を制御する。

### 3.6 Logging (`AudioEffector.Infrastructure.Logging`)
- **配置基準**: NLogロギングサービスおよび実行時インターセプト用透過的プロキシ・属性。
- **実装ルール**:
  - `NLogService.cs`: NLogを用いたロガー具象クラス。
  - `LoggingProxy.cs`: `DispatchProxy` を継承し、インターフェースメソッドの呼び出しと例外を透過的にロギングする。
  - `LogDescriptionAttribute.cs`: ログ出力時にメソッドの処理内容を明記するための属性。

---

## 4. DI（依存性注入）登録仕様

インフラストラクチャ層のクラスは、レイヤー固有の拡張メソッド `AddInfrastructureServices()` で一括登録します。

```csharp
namespace AudioEffector.Infrastructure;

public static class DependencyInjection
{
    public static IServiceCollection AddInfrastructureServices(this IServiceCollection services)
    {
        // 再生エンジンはデバイスリソースを単一で管理するため Singleton
        services.AddSingleton<IAudioEngine, NAudioPlaybackEngine>();

        // リポジトリはキャッシュとファイルI/O競合防止のため Singleton
        services.AddSingleton<ITrackRepository, JsonTrackRepository>();
        services.AddSingleton<IPlaylistRepository, JsonPlaylistRepository>();
        services.AddSingleton<IFavoriteRepository, JsonFavoriteRepository>();
        services.AddSingleton<ISettingsRepository, JsonSettingsRepository>();
        services.AddSingleton<IDataTransferAdapter, MtpDataTransferAdapter>();

        return services;
    }
}
```

---

## 5. 関連ドキュメント・Issue

- **全体設計書**: [アーキテクチャ全体設計書.md](アーキテクチャ全体設計書.md)
- **関連Issue**: [#132 (Phase 0)](https://github.com/taketonnbo/Audio_Effector/issues/132), [#134 (Phase 2)](https://github.com/taketonnbo/Audio_Effector/issues/134), [#148 (Refactor 137-1)](https://github.com/taketonnbo/Audio_Effector/issues/148)
