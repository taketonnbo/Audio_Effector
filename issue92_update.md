- 機器を接続しても、接続した機器リストに何も表示されない。（Manager機能も起動しない）
- 機器を検出し、機器への楽曲データ転送を可能にする。

#### 対応範囲外
SELECT機能による、アルバムデータの転送は、別Issue対応とする。

---

### 1. 概要
機器（リムーバブルドライブやMTPデバイス）を接続しても、機器同期画面の「接続した機器リスト (CONNECTED DEVICE)」に何も表示されず、デバイスへの楽曲データ転送ができないバグを修正します。

### 2. 実装内容
デバイス検出とリスト更新のロジックが正しくトリガーされていないことが原因であるため、以下の修正を行います。
- **UI状態変更時のリスト更新** (`MainViewModel.cs`)
  - `SwitchViewCommand` にて、画面が機器同期画面 (`ViewType.DeviceSync`) に切り替わった際に `RefreshDrives()` を呼び出すように修正します。
  - `CurrentViewType` プロパティのセッターでも、値が `ViewType.DeviceSync` の場合は `RefreshDrives()` を実行するロジックを追加し、リストが確実に更新されるようにします。
- **古いプロパティの整理** (`MainViewModel.cs`)
  - 古いUIの表示判定プロパティである `IsDeviceSyncVisible` に依存していた処理を整理し、新しい `ViewType` ベースの表示切り替えロジックと統合します。
- **自動検出の連動** (`MainViewModel.cs`)
  - デバイスの定期ポーリング (`OnTimerTick`) または `IsDeviceConnected` の状態変化において、接続を検知した時点で必要に応じてデバイスリストをリフレッシュする処理を検討します。

### 3. 関連Issue
- 関連Issue: 機器転送時のSELECT（曲単位およびアルバム単位）機能の実装（新規作成）
